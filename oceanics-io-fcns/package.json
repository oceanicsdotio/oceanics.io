{
    "name": "oceanics-io-fcns",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "clean": "rimraf dist/",
        "copyfiles": "yarn clean && copyfiles -u 1 src/shared/pkg/* src/**/*.txt src/**/*.json dist",
        "build": "yarn copyfiles && tsc && yarn build:validator",
        "build:validator": "ajv compile --strict=false -s src/shared/bathysphere.json -o src/shared/validate.js"
    },
    "keywords": [],
    "author": "Oceanicsdotio LLC",
    "license": "MIT",
    "engines": {
        "node": "14"
    },
    "devDependencies": {
        "ajv-cli": "^5.0.0",
        "copyfiles": "^2.4.1",
        "rimraf": "^3.0.2",
        "typescript": "^4.5.4"
    }
}
