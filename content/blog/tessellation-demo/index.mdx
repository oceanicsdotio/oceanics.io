---
title: Grid and mesh visualization demo
date: "2020-09-06T17:39:00.000Z"
description: |
    Technical demonstration of interactive data structures.
tags: ["demo", "graphics", "rust", "mdx", "mesh", "grid"]
---

<RectilinearGrid 
    shape={[16, 16, 1]} 
    gridColor={`#421F33FF`}
    overlayColor={`#CCCCCCFF`}
    backgroundColor={`#00000066`}
    lineWidth={1.0}
    tickSize={20.0}
    fontSize={14.0}
    labelPadding={5.0}
    stencil={1}
/>
<StyledCaption>
    Rectilinear grids are trivial to index, and very fast, but are
    not particularly flexible.
</StyledCaption>
<br/>

<TriangularMesh
    fontSize={14.0}
    shape={[16,16]}
    meshColor={`#EF5FA1CC`}
    overlayColor={`#CCCCCCFF`}
    backgroundColor={`#00000088`}
    lineWidth={1.0}
    labelPadding={5.0}
    tickSize={20.0}
/>
<StyledCaption>
    Unstructured triangular meshes are where it's at. They are very fast to interpolate,
    and the topological information can be stored efficiently. Triangles can be used
    directly by graphics hardware without any intermediate steps. 
</StyledCaption>

<br/>
<HexagonalGrid
    font={`14px Arial`}
    shape={[12,12]}
    gridColor={`#EF5FA1FF`}
    overlayColor={`#77CCFFFF`}
    backgroundColor={`#00000088`}
    caption={`HexagonalGrid`}
    alpha={1.0}
    lineWidth={1.0}
/>
<StyledCaption>
    Hexagonal grids are all the rage, but are not numerically interesting. These are
    good for binning quantities in space. Having more neighbors reduces visual artifacts
    like striping in the X and Y dimensions. 
</StyledCaption>