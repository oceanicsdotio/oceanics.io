
var assert = require('assert');

describe('Auth API', function() {
  describe('Register', function() {
    it('should allowed registration with API key', function() {
      
    });
    it('should prevent registration without API key', function() {
      
    });
    it('should prevent registration with wrong API key', function() {
      
    });
  });

  describe('Get JWT', function() {
    it('should return well-formed token given credentials', function() {
      
    });
    it('should deny access without credentials', function() {
      
    });
    it('should deny access with wrong credentials', function() {
      
    });
  });
});
